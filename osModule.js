const os = require('os');

console.log(os.freemem()) //to know the free memory of the sytem
console.log(os.homedir()) //to know the homedirectry
console.log(os.hostname()) //to know the host name
console.log(os.platform()) //to know the operating system
console.log(os.release()) //release of the os
console.log(os.totalmem()) //to know the total memory
console.log(os.uptime()) //from when thye sytem is open, output in seconds
